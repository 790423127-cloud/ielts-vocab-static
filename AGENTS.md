# 全局工作规则

## 角色与沟通

- 默认使用中文，并把用户当作不懂代码的普通用户。
- 先直接说明结论，再解释根因、修改、验证和风险。
- 专业术语要说明含义及其实际影响。
- 不展示冗长的内部推理，但要提供关键依据、不确定性和验证结果。
- 不迎合错误前提；发现用户理解有误时直接更正。
- 不虚构读取、搜索、命令、测试、修改、部署或外部操作结果。

## 核心目标

- 首要目标是在安全、可回退的前提下，完整解决用户当前目标，而不是只做最表面的最小改动后提前结束。
- “最小修改”指不修改无关代码，不代表只能修改一个文件。
- 如果同一个问题的根因涉及多个文件、模块或调用方，应继续追踪并完成必要修改。
- 为定位问题，可以搜索、阅读和分析直接相关的代码、配置、日志、测试和调用链。阅读和搜索不属于扩大修改范围。
- 除非遇到真实阻塞、缺少关键权限或需要高风险确认，否则不要因为发现关联代码或需要继续调查而提前结束。

## 开始工作

代码任务开始时：

1. 读取当前目录适用的 `AGENTS.md` 或 `AGENTS.override.md`。
2. 阅读与任务直接相关的 README、配置、代码和测试。
3. 检查 Git 状态，识别用户已有的未提交修改。
4. 保留无关的已有修改，不覆盖、不回退、不格式化它们。
5. 工作区存在未提交修改本身不是停止调查的理由；只有存在覆盖风险时才说明并调整方案。

复杂任务应先形成简短计划，明确目标、初步范围、风险和验证方式，然后继续执行。初步范围可以根据调查结果调整。

## 排查与修改

- 优先定位根本原因，不盲目增加判断、状态、接口、组件或临时补丁。
- 无法完全确认根因时，基于代码、日志和复现结果提出最有证据的假设，并用最小、可逆的修改验证。
- 优先选择改动最少但足以完整解决问题的方案。
- 复用现有组件、函数、类型、接口、数据结构和设计系统。
- 不顺便重构、批量改名、移动目录、统一格式、升级依赖或修改无关功能。
- 同一个问题直接涉及的多个文件属于同一任务，不属于批量任务。
- 如果调查发现的是另一个独立问题，不顺便修改；在最终汇报中单独说明。
- 只有当修改会改变公共接口、共享类型或公共行为时，才检查所有相关调用方。
- 新实现完整替代旧实现时，检查引用和依赖后，删除确认无用的旧分支、重复逻辑、临时补丁、无效变量和无效引用。
- 无法确认旧代码是否仍被使用时，保留它并说明原因，不擅自删除。
- 不用大量 `if`、`try/catch`、`any`、强制类型断言、静默失败、临时延迟、重复请求或无限重试掩盖根因。
- 不新增核心依赖，除非现有方案确实无法合理实现，并已向用户说明必要性、影响和替代方案。

## 允许继续和必须暂停的边界

下列情况仍属于正常工作，可以直接继续：

- 搜索和阅读相关文件；
- 跨文件追踪同一个问题的调用链；
- 运行本地检查、测试和构建；
- 修改同一个问题直接涉及的多个文件；
- 修正由本次修改造成的测试、类型或构建错误；
- 检查差异并清理本次修改产生的无用代码。

只有下列情况需要先获得用户明确确认：

- 删除、覆盖或迁移重要数据；
- 修改真实生产环境或生产数据库；
- 修改认证、权限、支付、计费或用户数据隔离的安全模型；
- 产生真实费用或调用真实付费服务；
- 上传私密文件；
- 发送、发布、购买或提交线上内容；
- 提交、推送、合并、开 PR 或部署；
- 执行破坏性 Git 命令；
- 更换框架、数据库、认证系统或核心依赖；
- 大规模重构或任务范围变成新的独立项目。

请求确认时说明准备做什么、为什么需要、最坏影响、是否影响数据或费用，以及备份和回退方法。

## 验证

- 完成一个完整逻辑修改阶段后检查 `git diff`，确认只有本任务相关改动。
- 根据修改范围运行最小但充分的验证，例如针对性的类型检查、Lint、测试、构建或功能验证。
- 不必为局部修改默认运行整个测试套件，除非项目规则要求或修改影响全局。
- 验证失败时，先判断是否由本次修改造成。
- 如果由本次修改造成，继续修正或回退本次修改，不要通过无关补丁掩盖失败。
- 没有实际运行的检查不得声称通过。
- 工具或环境限制导致无法验证时，明确说明哪些内容尚未确认。

## 完成标准

只要没有真实阻塞，应持续工作到以下目标达到：

- 当前问题得到完整解决，而不是只隐藏症状；
- 没有修改无关功能；
- 没有覆盖用户已有修改；
- 没有重复或冲突实现；
- 相关验证已经实际运行，或明确说明无法运行的原因；
- 修改可以清楚回退。

最终用中文简洁说明：

1. 根本原因；
2. 修改了什么；
3. 删除了什么以及为什么删除；
4. 保留了什么以及为什么保留；
5. 实际运行了哪些检查及结果；
6. 尚未确认的内容和剩余风险；
7. 用户需要进行的简单验证；
8. 回退方法。

如果没有发生数据修改、费用、外部账号操作、发送、发布、提交、上传或部署，用一句话统一说明即可。

---

# AGENTS.md — IELTS 词库网站

## 1. 项目目标

这是 IELTS 词库与学习状态网站。修改时优先保护：

- 正式词库与稳定 ID；
- 词形、词族、释义、例句、搭配和分类；
- 收藏、学习进度、复习状态、SRS 与 IndexedDB；
- AI 队列、缓存、音频和密钥。

## 2. 默认边界

除非用户在当前任务中明确授权，否则：

- 只做本地修改；
- 不提交、推送、创建 PR、部署或发布；
- 不调用真实付费 AI；
- 不修改真实用户数据；
- 不删除正式词库、缓存、备份或本地数据库。

一次授权只对当前任务有效。

## 3. 任务要求与目标确认

当前任务中的要求默认累计生效；后续新增要求默认是追加，不是替换。

只有用户明确表示“删除前面的要求”“以后面的为准”“推翻原方案”或“重新开始”时，旧要求才失效。

修改前先确认：

- 用户实际使用的是哪个页面或功能；
- 真实运行入口、数据来源和调用链；
- 准备修改什么；
- 明确不修改什么。

不根据文件名、组件名或功能名称相似就假定找到了正确入口。

可以读取多个直接相关文件来确认调用链；读取范围可以大于修改范围。

如果存在正式网站、静态导出页或其他并行实现，必须确认用户实际使用的是哪一套。

## 4. 数据保护

保持 `word`、`id`、`wordId`、`forms` 和 `wordFamily` 稳定，除非当前任务明确要求并已确认安全。

不得静默丢字段、改变稳定 ID 或覆盖收藏、学习状态、SRS、IndexedDB 数据。

如果 `public/data/words.json` 与 `.static-export-cache/words.json` 都是正式来源，写入前必须确认两者一致。

删除、去重、合并、批量写回、导入导出或修改正式词库前，必须先说明备份、影响和回退方式，并取得明确确认。

用户明确要求修改教学内容时，可以修改与任务直接相关的字段；不得借机修改无关释义、分类、ID 或用户状态。

不要把“单词难度”和“例句所属 Section”混用；字段含义不明确时先检查调用方。

## 5. 代码修改原则

优先采用改动较小但能够完整解决问题的方案。

不把“修改文件少”置于“正确解决问题”之前。

修复根本原因，不在错误入口或旧实现上继续叠加补丁。

新方案完整替代旧方案时，先检查调用关系，再清理确认无用的旧实现。

新增事件、快捷键、按钮、状态或删除操作前，先检查是否已有实现，避免重复绑定或重复逻辑。

修改公共组件、API、数据结构或 Hook 前，先检查直接调用方。

不修改、格式化或重构与当前任务无关的代码。

不用大量 `any`、强制类型断言、复制代码或临时判断掩盖问题。

不静默吞掉错误，不虚构测试结果。

## 6. AI、费用与密钥

默认使用 mock、fixture、临时数据或隔离环境。

真实 DeepSeek 或其他付费 AI 调用默认为 0。

真实调用前必须说明预计词数、批量大小、并发、重试、费用、停止方式和写回字段，并取得明确授权。

必须支持中止；中止后不得继续请求或写回。

同一轮失败词不得无限重试或重复计费。

AI 返回必须经过结构、字段范围和业务规则校验；请求返回不等于任务成功。

不打印、上传、覆盖或泄露 API Key、Token、密码、`.env.local` 等敏感信息。

## 7. 验证原则

根据改动范围选择必要的检查，不机械运行所有命令。

修复 Bug 时，优先增加或运行能复现问题的定向测试。

必须验证用户实际使用的入口和最终产物，不能只验证源代码或未接入真实链路的工具函数。

涉及静态导出时，应确认修改真实进入最终导出文件，并在生成后的页面上验证功能。

涉及删除时，应确认删除的是正确数据对象，并验证其他词库、稳定 ID 和用户状态未受影响。

完成前至少检查：

- `git diff --check`
- `git status --short`
- 与本次改动直接相关的测试或构建

每项检查分别标明“通过、失败或未运行”；没有实际运行不得说通过。

## 8. 必须停止的情况

出现以下情况时停止写入、付费或发布操作，只继续只读诊断：

- 发现真实密钥；
- 正式数据无法解析或正式来源不一致；
- 稳定 ID 大量变化或用户数据可能被覆盖；
- 没有可靠备份或回退方式；
- 测试会调用未经授权的真实付费 API；
- 任务无法安全中止；
- 当前文件存在与本次修改直接冲突的不明改动；
- 用户未授权删除、上传、发布、部署或付费调用。

如果不明修改位于无关文件，不要因此停止整个任务；只避开无关修改。

## 9. 完成后汇报

用中文简要说明：

- 解决了什么；
- 修改了哪些文件；
- 用户会看到什么变化；
- 实际运行了哪些检查，以及通过、失败或未运行；
- 是否修改正式词库、IndexedDB、稳定 ID 或用户状态；
- 真实付费 API 调用次数；
- 是否提交、推送、部署或发布；
- 剩余风险和回退方法。
