# AGENTS.md — IELTS 词库网站

## 1. 产品范围

这是 IELTS 词库和学习状态网站。核心资产包括：

- 正式词库；
- 单词稳定 ID；
- 词形、词族、释义、例句、搭配和分类；
- 收藏、学习进度、复习状态和 SRS；
- 浏览器 IndexedDB 数据；
- AI 修复、分类补全和可选内容丰富；
- 音频或 TTS 缓存。

任何修改都必须优先保护正式词库、稳定 ID 和用户学习数据。

## 2. 默认工作模式

除非用户在当前任务中明确授权，否则：

- 只在本地修改；
- 不提交或推送 GitHub；
- 不创建或合并 PR；
- 不部署；
- 不发布正式词库；
- 不调用真实付费 AI；
- 不修改用户真实学习数据；
- 不删除缓存、备份或本地数据库。

一次授权只对当前任务有效，不自动延伸到后续任务。

## 3. 开始任务前

必须先：

1. 阅读 README、package.json 和相关代码。
2. 运行并检查：
   - `git status --short`
   - `git branch --show-current`
   - `git rev-parse HEAD`
3. 确认没有覆盖用户未提交的修改。
4. 只检查与本次任务直接相关的页面、组件、API、数据文件、IndexedDB、AI 队列和测试。
5. 除非用户明确要求“完整项目审计”，否则不要扫描整个仓库。
6. 第一次复现问题使用最小 fixture、mock AI、临时数据或隔离浏览器 Profile，不得直接拿完整真实词库做实验。

## 4. 不可破坏的数据规则

1. 保持 `word`、`id`、`wordId` 的稳定性。
2. 不得无理由重新生成、批量替换或丢失 ID。
3. 保持 `forms`、`wordFamily` 和有效分类字段。
4. 如果以下两个文件同时是正式来源，必须保持一致：
   - `public/data/words.json`
   - `.static-export-cache/words.json`
5. 发现两个正式词库不一致时，停止写入并先报告。
6. 收藏、学习状态、SRS 和 IndexedDB 不是可随意重建的缓存。
7. 导入导出必须可往返，不得静默丢字段、改变 ID 或覆盖用户状态。
8. 删除、去重、合并词条时，默认只生成候选列表和差异预览；用户确认后才能真正应用。
9. 涉及正式词库、IndexedDB、导入导出、批量写回、词形词族或删除合并前，必须先说明备份和回退方式。

## 5. 字段修改边界

### 精准结构修复

只修实际异常字段，并保留：

- `word`
- `id`
- `wordId`
- `forms`
- `wordFamily`
- 有效分类
- 收藏和学习状态

### 可选内容丰富

只允许修改：

- `collocations`
- `phraseCollocations`

不得修改：

- `meaning`
- `definition`
- `example`
- `exampleCn`
- `otherMeanings`
- `ieltsUse`
- `topics`
- `difficulty`
- `word`
- `id`
- `wordId`
- `forms`
- `wordFamily`
- 用户状态

### 分类补全

只允许修改：

- `ieltsUse`
- `topics`
- `difficulty`

不得借分类补全修改释义、例句、ID、词形、词族或用户状态。

## 6. AI 和费用规则

1. 默认使用 mock、fixture 或隔离测试数据。
2. 真实 DeepSeek 或其他付费 AI 调用次数默认为 0。
3. 真实调用前必须获得用户当前任务的明确授权，并说明：
   - 预计处理词数；
   - 批量大小；
   - 并发数；
   - 最大重试次数；
   - 预计费用；
   - 停止方式；
   - 写回字段。
4. 必须支持中止；中止后不得继续写回。
5. 同一轮失败词不得无限重试或重复计费。
6. AI 返回必须经过 schema、字段范围和业务规则验证。
7. “成功”必须表示修复后重新校验通过，不能只表示请求返回。
8. React 状态更新器中不得发网络请求、修改外部数组、增加外部计数器或决定付费回退。
9. 防止页面重复渲染导致重复 AI 请求。

## 7. 本地文件和密钥保护

不得擅自删除、覆盖、打印或上传：

- `.env.local`
- `.ai-cache`
- `public/data/words.json`
- `.static-export-cache/words.json`
- 浏览器 IndexedDB
- 完整词库备份
- 收藏、学习状态和 SRS
- 音频缓存
- API Key、Token、密码

不得运行：

```powershell
npm audit fix --force
```

不得为了“清理环境”删除 `.ai-cache`、正式词库或用户数据。

## 8. 代码修改规则

1. 每次只解决一个明确问题。
2. 使用最小修改，不顺便重构。
3. 优先复用已有组件、类型、工具函数和数据结构。
4. 不创建第二套重复实现。
5. 修复根本原因，不用大量 `any`、类型断言、复制代码或临时判断掩盖问题。
6. 修改公共组件、API 或数据结构前先检查所有调用方。
7. 前端必须处理加载、空数据、错误、成功、防重复提交和移动端状态。
8. 不静默吞掉错误。
9. 不虚构测试结果。

## 9. 相关回归要求

当任务涉及相关功能时，至少检查：

- “完成”不是占位符；
- `null hypothesis` 不是占位符；
- 整格 `null` 是占位符；
- 合法词头 `null` 不被误删；
- 仍异常的词不计成功；
- 缺失批量返回时有受控回退；
- 可选丰富不改核心字段；
- 分类补全不改释义；
- 中止后不再写回；
- 同轮失败词不重复收费；
- 双正式词库一致；
- 稳定 ID 不丢失；
- 导入导出往返无损。

修复 Bug 时优先添加能复现问题的回归测试。

## 10. 验证命令

根据改动范围选择执行。

普通代码修复：

```text
npm run lint:errors
npm test
npm run quality:all
npm run build
```

UI 修改增加：

```text
npm run test:e2e
```

正式词库修改增加：

```text
npm run lexicon:check
npm run audit:core
npm run test:wide
```

AI 队列修改还要运行相关 targeted tests。

完成前运行：

```text
git diff --check
git status --short
git diff --stat
```

每条命令必须分别记录“通过、失败或未运行”。没有实际运行不得说通过。

## 11. 必须停止的情况

出现以下情况时停止写入和付费操作，只继续只读诊断：

- 发现真实密钥；
- 正式 JSON 无法解析；
- 双正式词库不一致；
- 稳定 ID 大量消失；
- 浏览器真实数据可能被覆盖；
- 没有备份或回退方案；
- 测试会调用真实付费 API；
- 付费任务无法停止；
- 同一批失败词持续重试；
- 工作区存在不明用户修改；
- 用户未授权上传、发布或付费调用。

## 12. 完成后汇报

用中文说明：

- 解决了什么；
- 修改了哪些文件；
- 用户会看到什么变化；
- 实际运行了哪些检查；
- 哪些通过、失败或未运行；
- 正式词库修改：是/否；
- IndexedDB 修改：是/否；
- 稳定 ID 修改：是/否；
- 用户状态修改：是/否；
- 真实付费 API 调用：0 或具体次数；
- GitHub 提交、上传或部署：是/否；
- 剩余风险和回退方法。
