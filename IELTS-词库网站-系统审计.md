# IELTS 词库网站系统审计指令

## 审计目标

对 IELTS 词库网站进行一次系统审计，找出真实缺陷、错误调用链、重复实现、遗留代码、数据风险、静态导出问题和验证盲区。

本次审计以“发现问题并给出可靠修复建议”为目标，不为了凑清单，也不默认立即重构整个项目。

你拥有充分自主权，可以：

- 阅读与任务相关的代码、文档和测试；
- 搜索引用与调用关系；
- 运行本地检查、测试和构建；
- 使用 mock、fixture、临时数据或隔离环境复现问题；
- 检查静态导出产物；
- 自主决定审计深度和顺序。

默认只读审计。不要在审计过程中直接修改正式词库、IndexedDB、用户学习状态、正式数据库或线上环境。普通本地诊断文件和临时测试数据可以使用，但不要污染项目。

---

## 一、先确认项目真实结构

先阅读：

- `AGENTS.md`
- `README`
- `package.json`
- 与词库数据、学习状态、静态导出、AI 队列有关的文档
- 与主要页面和核心功能直接相关的代码与测试

不要机械扫描整个仓库。根据发现逐步扩大范围。

需要确认项目是否同时存在：

- 正式网站；
- 静态导出网站；
- 开发页面；
- 旧页面或备用入口；
- 多套相似的刷词、拼写、错词本或词库整理页面；
- 多份正式词库、缓存词库或导出副本。

对主要功能分别追踪：

```text
用户实际入口
→ 页面或路由
→ 组件与 Hook
→ 接口或数据读取
→ 数据文件或 IndexedDB
→ 构建与静态导出
→ 最终用户实际加载的文件
```

不得仅根据文件名、组件名或功能名称相似就认定找到了正确入口。

---

## 二、正式词库与数据结构审计

重点检查：

- `word`、`id`、`wordId` 是否稳定；
- 是否存在重复 ID、缺失 ID、格式异常或大量 ID 变化；
- `forms`、`wordFamily` 是否完整、正确且引用一致；
- 词性、释义、例句、搭配、分类和教学字段的数据类型是否一致；
- 是否存在空字符串、错误占位符、异常 `null`、损坏 JSON 或字段漂移；
- 是否存在完全重复词条、近似重复词条、错误合并或错误拆分；
- `public/data/words.json` 与 `.static-export-cache/words.json` 的关系是否清楚；
- 多个正式来源之间是否一致；
- 导入导出是否能无损往返；
- 旧数据升级后是否会静默丢字段；
- 例句 Section、单词难度、分类和学习状态是否被错误混用。

不要直接批量修改正式数据。发现问题时输出：

- 影响范围；
- 示例词条；
- 风险等级；
- 修复建议；
- 是否需要备份；
- 是否适合自动修复；
- 是否必须人工确认。

---

## 三、学习状态与 IndexedDB 审计

检查：

- 收藏、学习进度、复习状态、SRS、错词本和拼写记录是否各自使用正确的数据对象；
- 不同学习模式是否错误共用删除、重置或更新方法；
- “移出错词本”“删除学习记录”“删除正式词条”是否被清楚区分；
- 页面刷新、切换分类、切换模式和重新导入后状态是否稳定；
- IndexedDB 版本升级是否安全；
- 导入导出是否会覆盖用户状态；
- 删除最后一项、空列表、重复记录和异常记录时是否正确；
- 多标签页或重复渲染是否可能造成重复写入；
- 用户状态是否可能被缓存清理误删。

尤其检查所有删除按钮、快捷键和删除方法的调用链，确认它们操作的是正确数据对象。

---

## 四、页面、快捷键与交互审计

检查主要页面：

- 主词库刷词页；
- 拼写页；
- 错词本；
- 词库整理；
- 分类筛选；
- 收藏与复习入口；
- 移动端页面；
- 静态导出页面。

重点检查：

- 是否存在多个功能相似但行为不一致的入口；
- 是否存在重复按钮、重复状态或重复事件监听；
- D、Delete、方向键、回车等快捷键是否重复绑定或跨页面误触；
- 输入框聚焦、组合输入、弹窗打开时是否仍会误触快捷键；
- 删除后当前索引、下一个词、最后一个词和空状态是否正确；
- 分类切换后词表、索引、筛选条件和统计是否一致；
- 移动端滑动、滚动、点击和触摸区域是否真实可用；
- 页面文案是否与真实行为一致；
- 错误是否被静默吞掉。

发现重复实现时，确认哪一套是真实入口、哪一套是旧实现、哪一套已失效。

---

## 五、静态导出与最终产物审计

确认静态网站的真实导出链路：

```text
用户下载或导出入口
→ 导出 API
→ 生成器或修补器
→ ZIP
→ index.html、脚本和数据文件
→ 用户部署后的实际页面
```

重点检查：

- 静态页是否使用了与正式网站不同的代码；
- 修补器是否真的接入导出链路；
- 测试通过的工具函数是否实际被调用；
- 最终 ZIP 是否包含最新代码；
- Service Worker、缓存版本、CDN 或浏览器缓存是否可能导致旧版本继续运行；
- 静态页面是否显示可追踪的版本号或构建标识；
- 正式网站修复是否被错误当成静态网站修复；
- 静态页的分类、滑动、快捷键和学习状态是否在最终产物中实际可用。

不能只检查源文件或字符串。应尽量打开生成后的静态页面，执行真实交互。

---

## 六、AI 队列、费用与写回审计

检查：

- 是否默认使用 mock、fixture 或隔离环境；
- 页面重复渲染是否会造成重复 AI 请求；
- React 状态更新器内是否存在网络请求或外部副作用；
- 中止后是否仍继续请求或写回；
- 同一批失败词是否无限重试或重复计费；
- 缺失批量返回时是否有受控回退；
- AI 返回是否经过结构、字段范围和业务规则校验；
- “请求成功”是否被错误当成“修复成功”；
- 可选内容丰富是否越权修改核心字段；
- 分类补全是否修改了释义或例句；
- 缓存是否可能保存错误结果或跨任务复用不当；
- 密钥是否可能被打印、提交、上传或写入日志。

不得调用真实付费 AI 进行审计。

---

## 七、代码质量与遗留实现审计

重点寻找：

- 同一功能的多套实现；
- 已失效但仍被引用的旧组件、旧 Hook 或旧事件；
- 新旧逻辑同时生效；
- 临时转发层、兼容层和重复判断；
- 大量 `any`、强制类型断言、复制代码或宽泛异常处理；
- 静默失败；
- 为通过测试而添加的特殊分支；
- 页面逻辑与数据层职责混乱；
- 未使用但仍影响构建或维护的文件；
- 注释与真实行为不一致；
- PR 或任务标题与真实修改对象不一致。

不要仅凭“看起来没用”就建议删除。先检查引用和运行入口。

---

## 八、测试与验证有效性审计

检查现有测试是否真正证明用户功能有效：

- 是否只测试字符串替换，没有测试真实行为；
- 是否只测试正式网站，没有测试静态网站；
- 是否只测试组件，没有走真实用户入口；
- 是否只检查函数返回，没有检查最终数据变化；
- 是否遗漏移动端触摸、滑动和焦点场景；
- 是否遗漏删除后下一个词、最后一个词和空状态；
- 是否遗漏正式词库和用户状态未受影响；
- 是否存在测试绿但最终产物无效的情况；
- 是否存在从未运行或被安全策略阻止却被写成通过的检查。

根据发现运行最有价值的定向测试，不机械运行所有命令。

---

## 九、审计报告格式

按风险等级输出：

### P0：可能造成正式数据、用户状态、费用或安全事故

### P1：核心功能错误、修错入口、用户实际功能不可用

### P2：重复实现、遗留代码、测试盲区、维护风险

### P3：低风险体验、文案或结构问题

每个问题必须包含：

- 问题名称；
- 已确认事实；
- 证据位置；
- 用户实际影响；
- 根本原因；
- 建议修复方案；
- 预计影响范围；
- 建议验证方式；
- 是否需要用户确认；
- 不修复的风险。

同时给出：

1. 项目整体结论；
2. 最优先处理的 3—5 个问题；
3. 可以合并修复的问题；
4. 不建议现在处理的问题；
5. 疑似问题但证据不足的项目；
6. 推荐的修复顺序；
7. 建议清理的重复或遗留实现候选；
8. 建议补充的关键测试。

---

## 十、审计边界

审计过程中不得：

- 修改正式词库；
- 修改真实 IndexedDB 或用户状态；
- 调用真实付费 AI；
- 提交、推送、创建 PR、部署或发布；
- 执行破坏性 Git 操作；
- 因为发现问题而直接大规模重构。

可以：

- 阅读和搜索代码；
- 运行只读诊断；
- 使用临时数据；
- 运行本地测试和构建；
- 生成临时静态产物；
- 输出修复方案和候选差异。

没有实际检查或验证的内容，必须标为“尚未确认”，不得写成已确认问题。
