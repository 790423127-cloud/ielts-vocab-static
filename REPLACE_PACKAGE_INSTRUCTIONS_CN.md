# 精准结构修复与可选丰富替换包

本包用于替换本地项目中的对应源文件，不会自动覆盖浏览器中已有的词库数据。

## 修复内容

- “结构异常”改为字段级精准修复，并移除“完成”、`null hypothesis` 等正常内容造成的误报。
- 词头待修与结构异常彻底分开，页面数字与批量按钮使用同一队列。
- 只有真正退出异常队列的词才计入修复成功。
- 保留原词头、ID、词形、词族、收藏和学习进度。
- 结构损坏时只修损坏字段；疑似语义异常时只替换 AI 内容字段，不改词族。
- 新增“丰富可选项 · 最多100词”。
- 新增“连续丰富可选项 · 可暂停”。
- 可选丰富只合并搭配和句型，最多 4+4，不覆盖释义、例句、分类或词族。
- 连续任务会跳过本轮持续失败的词，避免无限重复扣费。

## 替换方法

1. 关闭本地网页。
2. 在运行项目的 PowerShell 中按 `Ctrl+C`。
3. 备份整个原项目文件夹。
4. 解压本替换包。
5. 将包内文件夹和文件复制到原项目根目录。
6. Windows 提示是否合并文件夹或替换同名文件时，选择“全部替换”。
7. 不要删除原项目中的 `.env.local`、`.ai-cache`、`public/data/words.json` 和浏览器数据。
8. 在项目根目录运行：

```powershell
Get-Process node -ErrorAction SilentlyContinue | Stop-Process -Force
Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue
npm install
npm run dev
```

9. 浏览器打开 `http://127.0.0.1:3000`，按 `Ctrl+Shift+R` 强制刷新。

## 推荐操作顺序

1. 下载完整词库备份。
2. 点击“精准修复结构异常 · 最多100词”。
3. 结构异常归零后，运行“AI仅补分类 / 难度”。
4. 分类归零后，再点击“丰富可选项 · 最多100词”测试一轮。
5. 确认数量正常下降后，再使用“连续丰富可选项 · 可暂停”。

## 注意

- 可选丰富会调用 DeepSeek API 并产生费用。
- 6895 个是原始统计；你本地已经处理过一部分，以页面当前数字为准。
- 连续丰富可能运行较久，期间不要刷新页面、关闭 PowerShell 或让电脑休眠。
- 完成后再次下载完整词库备份。
